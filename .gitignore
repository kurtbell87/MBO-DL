# Secrets & credentials
.env
.env.*
*.pem
*.key
credentials.json

# Master-kit runtime
.master-kit.env
.mcp-token

# OS files
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Build artifacts
build/
dist/
*.o
*.a
*.so
*.dylib

# Kit runtime artifacts (symlinks — resolved from orchestration/)
.kit/scripts/
.kit/tdd.sh
.kit/experiment.sh
.kit/math.sh
.kit/.dashboard/

# MBO-DL project data (49GB raw MBO data)
/DATA/
*.dbn.zst
*.zip

# C++ build artifacts
build/
*.o
*.a
*.so
*.dylib

# Large experiment artifacts now managed by artifact-store (S3 + symlinks).
# The actual data lives in .kit/.s3-cache/ (ignored below).
# Symlinks and .s3-manifest.json files are tracked in git.

# S3 artifact store local cache
.kit/.s3-cache/

# Clangd index cache
.cache/clangd/
