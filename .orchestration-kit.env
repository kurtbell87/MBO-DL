export PROJECT_ROOT="/Users/brandonbell/LOCAL_DEV/MBO-DL-02152026"
export ORCHESTRATION_KIT_ROOT="/Users/brandonbell/LOCAL_DEV/MBO-DL-02152026/orchestration-kit"
export KIT_STATE_DIR=".kit"
export ORCHESTRATION_KIT_MCP_HOST="127.0.0.1"
export ORCHESTRATION_KIT_MCP_PORT="7337"
export ORCHESTRATION_KIT_MCP_MAX_OUTPUT_BYTES="32000"
export ORCHESTRATION_KIT_MCP_TOKEN="fb5290fa1c1a66eacbab910f983f595ab8393174e9b422a46907bf12bae5f364"

# Cloud preference: "local" (default), "cloud-first", or "cloud-always".
# "cloud-first"  — prefer cloud for jobs >10 min est. wall time (faster with more vCPUs).
# "cloud-always" — cloud for everything above the 10 min overhead floor.
# export ORCHESTRATION_KIT_CLOUD_PREFERENCE="local"

# Compute target: "local" or "ec2". When ec2, RUN phases mandate cloud-run.
export COMPUTE_TARGET="ec2"

# Subtree upstream remote (used by tools/sync-upstream)
export ORCHESTRATION_KIT_UPSTREAM_REMOTE="orchestration-kit-upstream"
export ORCHESTRATION_KIT_UPSTREAM_BRANCH="main"

# ---------------------------------------------------------------------------
# Local overrides — secrets, machine-specific paths, personal preferences.
# .env.local is gitignored and never committed.
# ---------------------------------------------------------------------------
if [ -f "${PROJECT_ROOT}/.env.local" ]; then
    source "${PROJECT_ROOT}/.env.local"
fi
